import React,{useEffect,useRef,useState}from'react';import{createRoot}from'react-dom/client';import{io}from'socket.io-client';import'./styles.css';
const api=async(u,o={})=>{const r=await fetch('/api'+u,{...o,headers:{'Content-Type':'application/json',Authorization:`Bearer ${localStorage.getItem('together_token')}`,...(o.headers||{})}});const d=await r.json();if(!r.ok)throw Error(d.message||'Request failed');return d};
function Auth({done}){const[m,setM]=useState('login'),[f,setF]=useState({name:'',email:'',password:''}),[e,setE]=useState('');const go=async x=>{x.preventDefault();try{const d=await api('/auth/'+(m==='login'?'login':'register'),{method:'POST',body:JSON.stringify(f)});localStorage.setItem('together_token',d.token);done(d.user)}catch(x){setE(x.message)}};return <main className="auth"><div className="brand">Together<span>•</span></div><h1>Don't just call.<br/><em>Hang out.</em></h1><p className="muted">Your small virtual room for the people you care about.</p><form onSubmit={go}>{m==='register'&&<input placeholder="Your name" onChange={x=>setF({...f,name:x.target.value})}/>}<input type="email" placeholder="Email" onChange={x=>setF({...f,email:x.target.value})}/><input type="password" placeholder="Password (6+ characters)" onChange={x=>setF({...f,password:x.target.value})}/>{e&&<div className="error">{e}</div>}<button>{m==='login'?'Enter Together':'Create account'}</button></form><button className="link" onClick={()=>setM(m==='login'?'register':'login')}>{m==='login'?'New here? Create an account':'Already have an account? Login'}</button></main>}
function Remote({stream}){const r=useRef();useEffect(()=>{r.current.srcObject=stream},[stream]);return <div className="videoCard"><video ref={r} autoPlay playsInline/><span>Guest</span></div>}
function Room({room,user,s,onLeave}){const local=useRef(),stream=useRef(),pcs=useRef({}),[cam,setCam]=useState(true),[mic,setMic]=useState(true),[remote,setRemote]=useState([]),[msgs,setMsgs]=useState([]),[text,setText]=useState(''),[notice,setNotice]=useState('');
const make=async(uid,offer=false)=>{let pc=pcs.current[uid];if(pc)return pc;pc=new RTCPeerConnection({iceServers:[{urls:'stun:stun.l.google.com:19302'}]});pcs.current[uid]=pc;stream.current?.getTracks().forEach(t=>pc.addTrack(t,stream.current));pc.onicecandidate=e=>e.candidate&&s.emit('webrtc:signal',{roomId:room.id,target:uid,data:{type:'candidate',candidate:e.candidate}});pc.ontrack=e=>setRemote(a=>[...a.filter(x=>x.id!==uid),{id:uid,stream:e.streams[0]}]);if(offer){const o=await pc.createOffer();await pc.setLocalDescription(o);s.emit('webrtc:signal',{roomId:room.id,target:uid,data:o})}return pc};
useEffect(()=>{let alive=true;api(`/rooms/${room.id}/messages`).then(d=>setMsgs(d.messages));(async()=>{try{stream.current=await navigator.mediaDevices.getUserMedia({video:true,audio:true});if(alive)local.current.srcObject=stream.current}catch{setNotice('Camera/microphone unavailable. You can still use chat.')}s.emit('room:join',room.id)})();const users=ids=>ids.forEach(id=>make(id,true));const signal=async({from,data})=>{const pc=await make(from,false);if(data.type==='offer'){await pc.setRemoteDescription(data);const a=await pc.createAnswer();await pc.setLocalDescription(a);s.emit('webrtc:signal',{roomId:room.id,target:from,data:a})}else if(data.type==='answer')await pc.setRemoteDescription(data);else if(data.type==='candidate')try{await pc.addIceCandidate(data.candidate)}catch{}};const chat=m=>m.roomId===room.id&&setMsgs(x=>[...x,m]);const n=x=>{setNotice(x.text);setTimeout(()=>setNotice(''),2500)};s.on('room:users',users).on('webrtc:signal',signal).on('chat:message',chat).on('room:notice',n);return()=>{alive=false;s.off('room:users',users).off('webrtc:signal',signal).off('chat:message',chat).off('room:notice',n);stream.current?.getTracks().forEach(t=>t.stop());Object.values(pcs.current).forEach(p=>p.close());pcs.current={};};},[room.id]);
const send=e=>{e.preventDefault();if(text.trim()){s.emit('chat:send',{roomId:room.id,text});setText('')}};const toggle=k=>{stream.current?.getTracks().filter(t=>t.kind===k).forEach(t=>t.enabled=!t.enabled);k==='video'?setCam(x=>!x):setMic(x=>!x)};const share=async()=>{try{const x=await navigator.mediaDevices.getDisplayMedia({video:true}),t=x.getVideoTracks()[0];Object.values(pcs.current).forEach(p=>p.getSenders().find(q=>q.track?.kind==='video')?.replaceTrack(t));local.current.srcObject=x;t.onended=()=>{const v=stream.current?.getVideoTracks()[0];Object.values(pcs.current).forEach(p=>p.getSenders().find(q=>q.track?.kind==='video')?.replaceTrack(v));local.current.srcObject=stream.current}}catch{}};
return <div className="room"><header><div><strong>{room.name}</strong><small>Private room</small></div><button className="danger" onClick={onLeave}>Leave</button></header>{notice&&<div className="notice">{notice}</div>}<section className="stage"><div className="videos"><div className="videoCard"><video ref={local} autoPlay muted playsInline/><span>You · {user.name}</span></div>{remote.map(x=><Remote key={x.id} stream={x.stream}/>)}</div><div className="controls"><button onClick={()=>toggle('audio')}>{mic?'🎙️':'🔇'}</button><button onClick={()=>toggle('video')}>{cam?'📷':'🚫'}</button><button onClick={share}>🖥️ Share screen</button></div></section><aside className="chat"><h3>Room chat</h3><div className="messages">{msgs.map(m=><div className={m.userId===user.id?'mine':''} key={m.id}><b>{m.userName}</b><span>{m.text}</span></div>)}</div><form onSubmit={send}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Say something..."/><button>Send</button></form></aside></div>}
function App(){const[u,setU]=useState(null),[room,setRoom]=useState(null),[rooms,setRooms]=useState([]),[fr,setFr]=useState({friends:[],requests:[]}),[q,setQ]=useState(''),[found,setFound]=useState([]),[name,setName]=useState(''),[err,setErr]=useState(''),[s,setS]=useState(null);useEffect(()=>{if(localStorage.getItem('together_token'))api('/me').then(d=>setU(d.user)).catch(()=>localStorage.removeItem('together_token'))},[]);useEffect(()=>{if(!u)return;api('/rooms').then(d=>setRooms(d.rooms));api('/friends').then(setFr);const x=io({auth:{token:localStorage.getItem('together_token')}});setS(x);return()=>x.disconnect()},[u]);if(!u)return <Auth done={setU}/>;if(room&&s)return <Room room={room} user={u} s={s} onLeave={()=>setRoom(null)}/>;const create=async()=>{try{const d=await api('/rooms',{method:'POST',body:JSON.stringify({name:name||'Together Room'})});setRooms(x=>[...x,d.room]);setName('');setRoom(d.room)}catch(x){setErr(x.message)}};const search=async()=>setFound(await api('/users/search?q='+encodeURIComponent(q)));const add=async id=>{try{await api('/friends/request/'+id,{method:'POST'});setErr('Friend request sent.')}catch(x){setErr(x.message)}};const enter=async r=>setRoom((await api('/rooms/'+r.id+'/join',{method:'POST'})).room);const refresh=async()=>setFr(await api('/friends'));return <main className="dashboard"><header className="top"><div><div className="brand">Together<span>•</span></div><small>Welcome, {u.name}</small></div><button className="link" onClick={()=>{localStorage.removeItem('together_token');location.reload()}}>Logout</button></header>{err&&<div className="error banner">{err}</div>}<div className="grid"><section className="panel hero"><h1>Your people.<br/><em>Your room.</em></h1><p>Start a private room and invite a friend. Video, audio and chat happen together.</p><div className="create"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Room name"/><button onClick={create}>Create room</button></div></section><section className="panel"><h2>Find friends</h2><div className="search"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Name or email"/><button onClick={search}>Search</button></div>{found.map(x=><div className="row" key={x.id}><span><b>{x.name}</b><small>{x.email}</small></span><button onClick={()=>add(x.id)}>Add</button></div>)}</section><section className="panel"><h2>Your rooms</h2>{rooms.map(r=><div className="row" key={r.id}><span><b>{r.name}</b><small>{r.members.length} member{r.members.length!==1?'s':''}</small></span><button onClick={()=>enter(r)}>Enter</button></div>)}{!rooms.length&&<p className="muted">Create your first room.</p>}</section><section className="panel"><h2>Friends</h2>{fr.friends.map(x=><div className="row" key={x.id}><span><b>{x.name}</b><small>{x.email}</small></span></div>)}{fr.requests.map(x=><div className="row" key={x.id}><span><b>{x.user.name}</b><small>Friend request</small></span><button onClick={async()=>{await api('/friends/'+x.id+'/accept',{method:'POST'});refresh()}}>Accept</button></div>)}{!fr.friends.length&&!fr.requests.length&&<p className="muted">Add someone to get started.</p>}</section></div></main>}
createRoot(document.getElementById('root')).render(<App/>);