# Phase 3 AI Question Engine

The current Question Lab works without an API key: it stores questions, categorizes them and calculates lexical similarity so repeated questions can be detected.

For live generative AI, configure `GEMINI_API_KEY` in the local server environment. The project can then connect a server-side Gemini integration without exposing the key to the browser.
